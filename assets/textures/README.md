# Background texture

Drop the **walnut plank** background image here as:

```
walnut.jpg
```

- Seamless/tileable is ideal (it repeats crisply); a single large photo also works (it'll be `cover`-fit).
- Dark walnut tone is best so the gold text and green felt stay readable.
- `.jpg` preferred; `.png` is fine. ~1500px+ wide.

If the filename differs, that's fine — just tell me what it is and I'll wire it up.
