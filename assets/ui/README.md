# UI / screen art

Drop the **home / setup screen** image here as:

```
setup-bg.jpg
```

This is shown on the screen where the player chooses their character, name, and
opponents (before the game starts).

- Tell me how you want it used: a **full background** behind the setup panel, or a
  **banner/header** image above it. I'll default to a full background with a dark
  overlay so the form stays readable.
- Landscape orientation works best for a background; ~1600px+ wide. `.jpg` preferred.

If the filename differs, just tell me and I'll wire it up.
